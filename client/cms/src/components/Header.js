import React from 'react';

const Header = () => {
    return (
        <div className="header">
            <button className="homebutton">Home</button>
            <button className="logoutbutton">Logout</button>
            <h2>MaricopaHealthy CMS Dashboard</h2>

        </div>
    );
};

export default Header;
