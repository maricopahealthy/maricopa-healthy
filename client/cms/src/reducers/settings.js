const settings = (state = [], action) => {
    switch (action.type) {
        case 'SAFARI_BROWSER':
            return state

        default:
            return state
    }
}
export default settings
