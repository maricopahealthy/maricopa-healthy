const initState = {
  subscriptions: []
}

const subscriptionsReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default subscriptionsReducer